const userTypes = ['user', 'company'];
const paramTypes = { 101: 'Integer', 102: 'Decimal', 103: 'Text', 104: 'Boolean' };
const featureTypes = ['export3D', 'interactWithInput', 'output', 'exportReport', 'financialFeasibility'];

module.exports = {
  userTypes,
  paramTypes,
  featureTypes,
};
